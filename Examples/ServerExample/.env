# Shared defaults, committed. Note PORT is absent, so the `default:` on the
# property supplies it.
API_URL=https://api.example.com
VERBOSE=false
ALLOWED_HOSTS=localhost,127.0.0.1
API_KEY=development-key
